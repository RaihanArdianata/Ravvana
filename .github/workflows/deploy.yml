name: Deploy Portfolio Web

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, nawasena]

    steps:
      - name: Deploy VPS
        run: |
          cd ~/apps/portfolio-web
          git pull
          docker compose down
          docker compose up -d --build
