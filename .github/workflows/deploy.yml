name: Deploy Portfolio Web

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: [self-hosted, nawasena, nawasena-runner]

    steps:
      - name: Deploy VPS
        run: |
          cd ~/apps/portfolio-web
          git pull
          docker compose down
          docker compose up -d --build
